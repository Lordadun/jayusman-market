@extends('layouts.app')
@section('title', 'Mutasi Stok')
@section('content')
<div class="flex flex-col md:flex-row md:justify-between md:items-center gap-4 mb-6"><div><h1 class="text-2xl font-bold">Mutasi Stok</h1><p class="text-gray-500">Riwayat keluar masuk barang.</p></div><a href="{{ route('stock-mutations.create') }}" class="bg-blue-600 text-white px-4 py-2 rounded-xl text-center">Tambah Mutasi</a></div>
<form method="GET" class="bg-white p-4 rounded-2xl shadow-sm mb-4 flex gap-2"><select name="type" class="border rounded-xl p-2"><option value="">Semua Tipe</option><option value="in" @selected(request('type')==='in')>Masuk</option><option value="out" @selected(request('type')==='out')>Keluar</option></select><button class="bg-slate-900 text-white px-4 py-2 rounded-xl">Filter</button></form>
<div class="bg-white rounded-2xl shadow-sm overflow-x-auto"><table class="w-full text-sm"><thead class="bg-gray-100"><tr><th class="p-3 text-left">Tanggal</th><th class="p-3 text-left">Produk</th><th class="p-3 text-left">Cabang</th><th class="p-3 text-center">Tipe</th><th class="p-3 text-center">Qty</th><th class="p-3 text-center">Sebelum</th><th class="p-3 text-center">Sesudah</th><th class="p-3 text-left">User</th><th class="p-3 text-left">Keterangan</th></tr></thead><tbody>@forelse($mutations as $mutation)<tr class="border-b"><td class="p-3">{{ $mutation->created_at->format('d/m/Y H:i') }}</td><td class="p-3 font-semibold">{{ $mutation->product->name ?? '-' }}</td><td class="p-3">{{ $mutation->branch->name ?? '-' }}</td><td class="p-3 text-center">@if($mutation->type==='in')<span class="bg-green-100 text-green-700 px-3 py-1 rounded-full">Masuk</span>@else<span class="bg-red-100 text-red-700 px-3 py-1 rounded-full">Keluar</span>@endif</td><td class="p-3 text-center">{{ $mutation->quantity }}</td><td class="p-3 text-center">{{ $mutation->stock_before }}</td><td class="p-3 text-center">{{ $mutation->stock_after }}</td><td class="p-3">{{ $mutation->user->name ?? '-' }}</td><td class="p-3">{{ $mutation->description }}</td></tr>@empty<tr><td colspan="9" class="p-5 text-center text-gray-500">Belum ada mutasi stok.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $mutations->links() }}</div>
@endsection
