@extends('layouts.app')
@section('title', 'Laporan Transaksi')
@section('content')
<div class="flex flex-col md:flex-row md:justify-between md:items-center gap-4 mb-6"><div><h1 class="text-2xl font-bold">Laporan Transaksi</h1><p class="text-gray-500">Filter dan cetak laporan transaksi berdasarkan tanggal.</p></div><a href="{{ route('reports.transactions.print', request()->query()) }}" target="_blank" class="bg-green-600 text-white px-4 py-2 rounded-xl text-center">Cetak</a></div>
<form method="GET" class="bg-white p-4 rounded-2xl shadow-sm mb-4 flex flex-col md:flex-row gap-2"><input type="date" name="start_date" value="{{ request('start_date') }}" class="border rounded-xl p-2"><input type="date" name="end_date" value="{{ request('end_date') }}" class="border rounded-xl p-2"><button class="bg-slate-900 text-white px-4 py-2 rounded-xl">Filter</button></form>
<div class="bg-white rounded-2xl shadow-sm overflow-x-auto"><table class="w-full text-sm"><thead class="bg-gray-100"><tr><th class="p-3 text-left">Invoice</th><th class="p-3 text-left">Cabang</th><th class="p-3 text-left">Kasir</th><th class="p-3 text-right">Total</th><th class="p-3 text-left">Tanggal</th></tr></thead><tbody>@php $grandTotal=0; @endphp @forelse($transactions as $transaction) @php $grandTotal += $transaction->total_price; @endphp<tr class="border-b"><td class="p-3">{{ $transaction->invoice_number }}</td><td class="p-3">{{ $transaction->branch->name ?? '-' }}</td><td class="p-3">{{ $transaction->cashier->name ?? '-' }}</td><td class="p-3 text-right">Rp {{ number_format($transaction->total_price,0,',','.') }}</td><td class="p-3">{{ $transaction->transaction_date }}</td></tr>@empty<tr><td colspan="5" class="p-5 text-center text-gray-500">Tidak ada data.</td></tr>@endforelse<tr class="bg-gray-100 font-bold"><td colspan="3" class="p-3 text-right">Total Pendapatan</td><td class="p-3 text-right">Rp {{ number_format($grandTotal,0,',','.') }}</td><td></td></tr></tbody></table></div>
@endsection
