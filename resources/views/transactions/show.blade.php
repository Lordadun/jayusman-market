@extends('layouts.app')
@section('title', 'Detail Transaksi')
@section('content')
<div class="flex justify-between items-center mb-6"><div><h1 class="text-2xl font-bold">Detail Transaksi</h1><p class="text-gray-500">{{ $transaction->invoice_number }}</p></div><button onclick="window.print()" class="bg-green-600 text-white px-4 py-2 rounded-xl">Cetak</button></div>
<div class="bg-white p-6 rounded-2xl shadow-sm mb-6 grid grid-cols-1 md:grid-cols-2 gap-4"><div><p class="text-gray-500">Cabang</p><p class="font-semibold">{{ $transaction->branch->name ?? '-' }}</p></div><div><p class="text-gray-500">Kasir</p><p class="font-semibold">{{ $transaction->cashier->name ?? '-' }}</p></div><div><p class="text-gray-500">Tanggal</p><p class="font-semibold">{{ $transaction->transaction_date }}</p></div><div><p class="text-gray-500">Invoice</p><p class="font-semibold">{{ $transaction->invoice_number }}</p></div></div>
<div class="bg-white rounded-2xl shadow-sm overflow-x-auto mb-6"><table class="w-full text-sm"><thead class="bg-gray-100"><tr><th class="p-3 text-left">Produk</th><th class="p-3 text-center">Qty</th><th class="p-3 text-right">Harga</th><th class="p-3 text-right">Subtotal</th></tr></thead><tbody>@foreach($transaction->details as $detail)<tr class="border-b"><td class="p-3">{{ $detail->product->name ?? '-' }}</td><td class="p-3 text-center">{{ $detail->quantity }}</td><td class="p-3 text-right">Rp {{ number_format($detail->price,0,',','.') }}</td><td class="p-3 text-right">Rp {{ number_format($detail->subtotal,0,',','.') }}</td></tr>@endforeach<tr class="bg-gray-100 font-bold"><td colspan="3" class="p-3 text-right">Total</td><td class="p-3 text-right">Rp {{ number_format($transaction->total_price,0,',','.') }}</td></tr><tr><td colspan="3" class="p-3 text-right">Bayar</td><td class="p-3 text-right">Rp {{ number_format($transaction->paid_amount,0,',','.') }}</td></tr><tr><td colspan="3" class="p-3 text-right">Kembalian</td><td class="p-3 text-right">Rp {{ number_format($transaction->change_amount,0,',','.') }}</td></tr></tbody></table></div>
<a href="{{ route('transactions.index') }}" class="bg-gray-500 text-white px-4 py-2 rounded-xl">Kembali</a>
@endsection
