@extends('layouts.app')
@section('title', 'Transaksi')
@section('content')
<div class="flex flex-col md:flex-row md:justify-between md:items-center gap-4 mb-6"><div><h1 class="text-2xl font-bold">Data Transaksi</h1><p class="text-gray-500">Riwayat transaksi penjualan.</p></div><a href="{{ route('transactions.create') }}" class="bg-blue-600 text-white px-4 py-2 rounded-xl text-center">Tambah Transaksi</a></div>
<form method="GET" class="bg-white p-4 rounded-2xl shadow-sm mb-4 flex flex-col md:flex-row gap-2"><input type="date" name="start_date" value="{{ request('start_date') }}" class="border rounded-xl p-2"><input type="date" name="end_date" value="{{ request('end_date') }}" class="border rounded-xl p-2"><button class="bg-slate-900 text-white px-4 py-2 rounded-xl">Filter</button></form>
<div class="bg-white rounded-2xl shadow-sm overflow-x-auto"><table class="w-full text-sm"><thead class="bg-gray-100"><tr><th class="p-3 text-left">Invoice</th><th class="p-3 text-left">Cabang</th><th class="p-3 text-left">Kasir</th><th class="p-3 text-right">Total</th><th class="p-3 text-right">Bayar</th><th class="p-3 text-right">Kembalian</th><th class="p-3 text-left">Tanggal</th><th class="p-3">Aksi</th></tr></thead><tbody>@forelse($transactions as $transaction)<tr class="border-b"><td class="p-3 font-semibold">{{ $transaction->invoice_number }}</td><td class="p-3">{{ $transaction->branch->name ?? '-' }}</td><td class="p-3">{{ $transaction->cashier->name ?? '-' }}</td><td class="p-3 text-right">Rp {{ number_format($transaction->total_price,0,',','.') }}</td><td class="p-3 text-right">Rp {{ number_format($transaction->paid_amount,0,',','.') }}</td><td class="p-3 text-right">Rp {{ number_format($transaction->change_amount,0,',','.') }}</td><td class="p-3">{{ $transaction->transaction_date }}</td><td class="p-3 text-center"><a href="{{ route('transactions.show',$transaction) }}" class="bg-blue-100 text-blue-700 px-3 py-1 rounded-lg">Detail</a></td></tr>@empty<tr><td colspan="8" class="p-5 text-center text-gray-500">Belum ada transaksi.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $transactions->links() }}</div>
@endsection
